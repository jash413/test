import React from 'react';

const CreateNewChatModal = ({
  open,
  setOpen
}: {
  open: boolean;
  setOpen: (val: boolean) => void;
}) => {
  return (
    <div>
      <h1>CreateNewChatModal</h1>
    </div>
  );
};

export default CreateNewChatModal;
